/* ===== C++ Templates — Overloading & Specialization ===== */
window.QUIZZES = window.QUIZZES || {};
window.QUIZZES["templ-overload"] = {
  title: "C++ Templates — Overloading & Specialization",
  subtitle: "Function-template overloading & partial ordering, class-template full/partial specialization, and why specializing function templates is a trap.",
  crumb: "C++ Templates",
  questions: [
    {"type":"mcq","tag":"Overload Resolution","question":"When a non-template function and a function-template specialization are both exact, equally good matches for a call, which one does overload resolution choose?","options":["The function-template specialization, because it is more specific","The non-template function, because a non-template is preferred over a template when both are equally good","It is ambiguous and the program is ill-formed","Whichever is declared last in the translation unit"],"answer":1,"explain":"Overload resolution first compares conversion sequences. When those tie, a non-template function is preferred over any function template specialization. Only if both are templates does partial ordering come into play."},
    {"type":"mcq","tag":"Partial Ordering","question":"When two function templates could both be used for a call, how does the compiler pick one?","options":["By whichever was declared first","By partial ordering, choosing the 'more specialized' template","By preferring the one with fewer template parameters","It is always ambiguous"],"answer":1,"explain":"Partial ordering of function templates ranks them by how specialized they are. The more specialized template — the one whose parameters can be deduced from the other but not vice versa — is preferred."},
    {"type":"code","tag":"Template vs Non-template","question":"Which function is called by f(1)?","code":"template <typename T> void f(T) {}   // (1)\nvoid f(int) {}                        // (2)\n\nint main() { f(1); }","options":["(1) — the template deduces T = int and matches exactly","(2) — a non-template is preferred when both match equally well","Ambiguous: both are exact matches for int","(1) — templates always win over non-templates"],"answer":1,"explain":"Both candidates are exact matches for an int argument. The tie-breaker rule prefers the non-template f(int) over the template specialization f<int>."},
    {"type":"mcq","tag":"Partial Specialization","question":"Which of these language features does C++ NOT allow?","options":["Partial specialization of a class template","Full (explicit) specialization of a class template","Partial specialization of a function template","Full (explicit) specialization of a function template"],"answer":2,"explain":"Function templates cannot be partially specialized. To get partial-specialization-like behavior for functions you overload additional function templates instead."},
    {"type":"mcq","tag":"Function Templates","question":"Why does C++ forbid partial specialization of function templates but allow it for class templates?","options":["It is purely a historical oversight with no rationale","Function overloading already provides the needed mechanism, so partial specialization would be redundant and would complicate overload resolution","Functions cannot have template parameters","Because functions have no members to specialize"],"answer":1,"explain":"Overloading additional function templates achieves the same effect as partial specialization would, and it integrates cleanly with overload resolution and partial ordering. Adding a separate partial-specialization mechanism would only create ambiguity."},
    {"type":"code","tag":"Partial Ordering","question":"Which template is called by f(&x) where x is an int?","code":"template <typename T> void f(T)  {}   // (1)\ntemplate <typename T> void f(T*) {}   // (2)\n\nint main() { int x; f(&x); }","options":["(1) with T = int*","(2) with T = int, because T* is more specialized than T","Ambiguous: both deduce successfully","(1), because it was declared first"],"answer":1,"explain":"Both templates match &x, but T* is more specialized than the unconstrained T (any pointer argument that matches T* also matches T, not the reverse). Partial ordering selects (2)."},
    {"type":"mcq","tag":"Specialize vs Overload","question":"The usual advice for customizing a function template's behavior for certain types is to:","options":["Fully specialize the function template","Add an overloaded function (or function template) instead of specializing","Partially specialize the function template","Use a macro to switch implementations"],"answer":1,"explain":"Overloading participates in overload resolution and partial ordering, whereas an explicit specialization does not add a new candidate — it only refines an already-selected primary template. Overloading is more flexible and avoids surprising interactions."},
    {"type":"mcq","tag":"Dimov/Abrahams","question":"The Dimov/Abrahams example demonstrates that an explicit function-template specialization:","options":["Always wins over any overload","Does not participate in overload resolution directly; only its base template does, so which template it specializes can matter surprisingly","Is chosen only when declared first","Behaves identically to a class-template specialization"],"answer":1,"explain":"A specialization is bound to one particular base template. Overload resolution picks a base template first; a specialization of a different base template is then never considered, even if it looks like a better match for the argument."},
    {"type":"code","tag":"Function Specialization","question":"Does this compile, and if so what does it do?","code":"template <typename T> void f(T) {}\n\ntemplate <typename T>\nvoid f<T*>(T*) {}   // intended 'partial specialization'\n\nint main() { int x; f(&x); }","options":["Compiles; f<T*> is chosen for pointers","Ill-formed: function templates cannot be partially specialized","Compiles; it is a full specialization of f","Compiles but f<T*> is never actually called"],"answer":1,"explain":"The syntax f<T*> attempts a partial specialization of a function template, which the language forbids. The correct approach is a separate overload: template <typename T> void f(T*)."},
    {"type":"mcq","tag":"Explicit Specialization","question":"An explicit specialization for a given set of template arguments must be declared:","options":["Anywhere in the program, order does not matter","Before the first use that causes implicit instantiation for those arguments, in every translation unit that uses it","Only in the same file as the primary template","After all uses, at the end of the file"],"answer":1,"explain":"If a template is implicitly instantiated first and later given an explicit specialization for the same arguments, the program is ill-formed (often no diagnostic required). The specialization must be visible before first use."},
    {"type":"mcq","tag":"Tag Dispatch","question":"Tag dispatch selects an implementation by:","options":["Specializing the same function template repeatedly","Passing an extra argument of a distinct tag type so that overload resolution picks the matching overload","Using preprocessor #if branches","Runtime type checks with dynamic_cast"],"answer":1,"explain":"Tag dispatch adds an extra parameter whose type (a tag) encodes a compile-time property. Different tags select different overloads at compile time, avoiding both function-template specialization and heavy SFINAE."},
    {"type":"code","tag":"Full Specialization","question":"What is the correct syntax to fully specialize this class template for T = bool?","code":"template <typename T> struct Box { /* ... */ };","options":["template <bool> struct Box { ... };","template <> struct Box<bool> { ... };","template struct Box<bool> { ... };","specialize struct Box<bool> { ... };"],"answer":1,"explain":"A full (explicit) specialization uses an empty template parameter list, template<>, followed by the class name with the concrete arguments: template <> struct Box<bool> { ... }."},
    {"type":"mcq","tag":"SFINAE","question":"In overload resolution, SFINAE means that:","options":["A syntax error anywhere aborts the whole compilation","A substitution failure while forming a candidate's signature removes it from the overload set instead of being a hard error","Failed templates are always chosen last","Only class templates can fail silently"],"answer":1,"explain":"SFINAE — Substitution Failure Is Not An Error — means an invalid substitution in the immediate context of a candidate's signature merely discards that candidate, allowing other overloads to be selected."},
    {"type":"mcq","tag":"std::hash","question":"Which is a real rule/pitfall when specializing std::hash for your own type?","options":["You may only specialize it for fundamental types","You must specialize it for a program-defined type (not for a standard type you do not own), and it must not violate the standard's requirements (e.g. a valid, noexcept-friendly operator())","The specialization must live in your own namespace, never in namespace std","std::hash cannot be specialized at all"],"answer":1,"explain":"The standard permits users to specialize std::hash only for their own program-defined types, placed in namespace std, meeting the Hash requirements. Specializing it for a type you do not own is undefined behavior."},
    {"type":"code","tag":"Partial Specialization","question":"What does this partial specialization apply to?","code":"template <typename T> struct S { static const int v = 0; };\ntemplate <typename T> struct S<T*> { static const int v = 1; };","options":["Only S<void*>","Any instantiation S<P> where P is a pointer type","All instantiations of S","It is ill-formed"],"answer":1,"explain":"The partial specialization S<T*> matches any pointer type argument, deducing T as the pointee. So S<int*>::v and S<char*>::v are both 1, while S<int>::v is 0."},
    {"type":"mcq","tag":"Namespace std","question":"Adding a specialization of a standard library template such as std::hash for a user-defined type must be done:","options":["In the global namespace","Inside namespace std, and only for a type that depends on a program-defined type","Anywhere; namespace does not matter","It is never allowed"],"answer":1,"explain":"Users may add template specializations to namespace std only when the specialization depends on at least one program-defined type and meets the original template's requirements. std::hash is the canonical example."},
    {"type":"mcq","tag":"Specialization Basics","question":"An explicit specialization of a class template is best described as:","options":["A separate, complete definition used only for the specified template arguments; it need not resemble the primary template","A required override that must keep the same members as the primary","Just another overload","A runtime variant selected by if statements"],"answer":0,"explain":"A full specialization is an independent definition for particular arguments. It may have entirely different members, bases, and layout from the primary template — the compiler simply uses it whenever those exact arguments are requested."},
    {"type":"code","tag":"Dimov/Abrahams","question":"Which function handles g(&p) where p is an int (the Dimov/Abrahams gotcha)?","code":"template <typename T> void g(T);      // (1) primary\ntemplate <typename T> void g(T*);     // (2) overload\ntemplate <> void g<int*>(int*);       // (3) specialization\n\nint main() { int* p; g(p); }","options":["(3), because it is the most specialized","(2), and (3) specializes (1), so (3) is never chosen here","(1), the primary template","Ambiguous between (2) and (3)"],"answer":1,"explain":"Overload resolution first selects among the base templates (1) and (2); (2) wins by partial ordering. Only then are specializations of the chosen template considered — but (3) is a specialization of (1), not (2), so it is ignored. Ordering the declarations differently changes which function runs, which is the trap."},
    {"type":"mcq","tag":"Specialize vs Overload","question":"A key drawback of explicitly specializing a function template (versus overloading) is that the specialization:","options":["Runs slower at runtime","Does not add a new candidate to overload resolution; it only kicks in after the base template is already selected","Cannot access the arguments","Requires a virtual table"],"answer":1,"explain":"Overload resolution chooses among base templates and non-templates first. A specialization refines whichever base template was already picked, so a specialization can be silently bypassed if a different base template wins the initial resolution."},
    {"type":"mcq","tag":"Member Specialization","question":"When you specialize a single member function of a class template for a specific type, the syntax begins with:","options":["template <typename T>","template <> before the member definition, with the class name carrying the concrete argument","specialize","No prefix at all"],"answer":1,"explain":"A member specialization is written like template <> ReturnType ClassName<ConcreteArgs>::member(...) { ... }. The empty template<> marks it as an explicit specialization of that member."},
    {"type":"code","tag":"Member Specialization","question":"What does this specialize?","code":"template <typename T> struct C {\n    void f();\n    void g();\n};\n\ntemplate <> void C<int>::g() { /* ... */ }","options":["The entire class C<int>","Only the member g() for C<int>; f() still comes from the primary template","Both f() and g() for C<int>","It is ill-formed; you must specialize the whole class"],"answer":1,"explain":"You may specialize a single member of a class template without specializing the whole class. Here only C<int>::g has a custom definition; C<int>::f is generated from the primary template as usual."},
    {"type":"mcq","tag":"Partial Ordering","question":"Partial ordering determines that template A is 'more specialized' than template B when:","options":["A has more template parameters than B","B's parameters can be deduced from A's synthesized arguments, but not vice versa","A appears earlier in the file","A has a shorter mangled name"],"answer":1,"explain":"Conceptually, the compiler synthesizes arguments for each template and tries to deduce the other. If A can 'stand in for' B (B deduces from A) but B cannot stand in for A, then A is the more specialized template."},
    {"type":"mcq","tag":"Specialization Basics","question":"A full class-template specialization and the primary template must agree on:","options":["The set of member functions","Only the name and template argument count/kinds; the members and layout may differ entirely","The base classes","The size in bytes"],"answer":1,"explain":"A specialization only needs to match the primary in name and the shape of its template parameter list. Its internals — members, bases, size — are completely free to differ, which is why std::vector<bool> can be so different from std::vector<T>."},
    {"type":"code","tag":"Explicit Specialization","question":"What is wrong with this program?","code":"template <typename T> struct Box { T v; };\n\nBox<int> b;                 // first use\n\ntemplate <> struct Box<int> // specialize afterwards\n{ int v; long extra; };","options":["Nothing; specializations may appear anywhere","Ill-formed: the specialization of Box<int> appears after Box<int> was already used (implicitly instantiated)","Ill-formed: you cannot add members in a specialization","It compiles but b uses the primary template silently"],"answer":1,"explain":"An explicit specialization must be declared before the first use that would implicitly instantiate the template for those arguments. Using Box<int> first commits to the implicit instantiation, so the later specialization is an error."},
    {"type":"mcq","tag":"Function Templates","question":"Which statement about specializing function templates is TRUE?","options":["Full specialization is allowed; partial specialization is not","Partial specialization is allowed; full specialization is not","Both full and partial specialization are allowed","Neither full nor partial specialization is allowed"],"answer":0,"explain":"Function templates may be fully (explicitly) specialized, but not partially specialized. For partial-specialization-like behavior, add overloaded function templates instead."},
    {"type":"mcq","tag":"Dimov/Abrahams","question":"The safest way to avoid the Dimov/Abrahams surprise when you want type-specific behavior for a function is to:","options":["Always put specializations before overloads","Avoid specializing function templates; provide overloads (or overloaded templates) so everything is decided by overload resolution","Use only full specializations, never overloads","Declare the specialization inside a class"],"answer":1,"explain":"Because a specialization attaches to a single base template that may not be the one selected, relying on overloads keeps every candidate in the same resolution process, eliminating the ordering-dependent surprise."},
    {"type":"code","tag":"Tag Dispatch","question":"In this classic tag-dispatch pattern, which advance() overload runs for a std::random_access_iterator_tag?","code":"void advance_impl(Iter&, Dist, std::input_iterator_tag);         // (1)\nvoid advance_impl(Iter&, Dist, std::random_access_iterator_tag); // (2)\n\ntemplate <typename Iter, typename Dist>\nvoid advance(Iter& it, Dist n) {\n    advance_impl(it, n, typename std::iterator_traits<Iter>::iterator_category{});\n}","options":["(1), because input_iterator_tag is the base and matches directly","(2), an exact match; if absent, (1) would be chosen via the tag's base class","Neither; tag dispatch needs specialization","Both, causing ambiguity"],"answer":1,"explain":"random_access_iterator_tag matches overload (2) exactly. The tag hierarchy means that if (2) did not exist, the derived-to-base conversion would fall back to (1) — a deliberate design of the iterator category tags."},
    {"type":"mcq","tag":"SFINAE","question":"SFINAE only applies to failures that occur:","options":["Anywhere in the program during any phase","In the immediate context of template argument substitution (e.g. the function signature), not inside the function body","Only at link time","Only for class templates"],"answer":1,"explain":"A substitution failure must be in the immediate context — the signature, template parameters, return type. Errors that arise from instantiating the body are hard errors, not SFINAE."},
    {"type":"mcq","tag":"Specialize vs Overload","question":"One reason class-template partial specialization is safe while function 'partial specialization by overload' can surprise you is that:","options":["Class specializations are chosen after a single, unambiguous partial-ordering step tied to the one class template, whereas function overloads first go through overload resolution which may pick a different base template","Classes are always faster","Functions cannot be templates","Overloading is disabled for templates"],"answer":0,"explain":"A class template has one primary and its partial specializations are ranked directly against each other. Functions mix templates and non-templates in overload resolution first, so a specialization tied to a losing base template can be skipped."},
    {"type":"code","tag":"SFINAE","question":"What determines which overload of f is viable here?","code":"template <typename T>\nauto f(T x) -> decltype(x.size(), void()) {}   // (1)\ntemplate <typename T>\nvoid f(...) {}                                  // (2) fallback\n\nint main() { f(42); }","options":["(1), because int can be size()d","(2), because decltype(x.size()) is invalid for int, so (1) is removed by SFINAE","Ambiguous","Compilation error: int has no size()"],"answer":1,"explain":"For an int, x.size() is ill-formed, so substitution fails in (1)'s return type and it drops out of the overload set. The variadic fallback (2) remains and is chosen — no hard error occurs."},
    {"type":"mcq","tag":"Tag Dispatch","question":"Compared to SFINAE-based overload selection, tag dispatch is often preferred because:","options":["It works at runtime","It is usually clearer and produces better error messages, selecting an overload via a simple tag argument rather than complex enable_if conditions","It requires no function overloads","It only works with class templates"],"answer":1,"explain":"Tag dispatch expresses the choice as ordinary overload resolution on a tag type, which reads more clearly and yields friendlier diagnostics than enable_if-laden signatures, while achieving the same compile-time dispatch."},
    {"type":"mcq","tag":"Explicit Specialization","question":"The declaration 'template <>' introduces:","options":["A variadic template","An explicit (full) specialization, whose empty parameter list signals that all template parameters are fixed by the given arguments","A partial specialization","A template with zero requirements"],"answer":1,"explain":"template<> with an empty angle-bracket list marks a full specialization: no template parameters remain because every one is pinned down by the explicit arguments after the entity's name."},
    {"type":"code","tag":"std::hash","question":"What is the correct way to make a user type Point usable as a std::unordered_map key by specializing std::hash?","code":"struct Point { int x, y; };","options":["template <> struct std::hash<Point> { size_t operator()(const Point&) const; };","Overload operator std::hash for Point in the global namespace","Inherit Point from std::hash","You cannot hash user types; use std::map instead"],"answer":0,"explain":"You add a full specialization of std::hash for your type, providing a const operator() returning size_t. This is one of the few standard templates users are explicitly permitted to specialize."},
    {"type":"mcq","tag":"std::hash","question":"For std::unordered_map<Key, V> to work with a custom Key, you need Key to have:","options":["Only operator< defined","A hash (e.g. a std::hash specialization or a supplied hash functor) AND operator== for equality","Only operator== defined","A default constructor only"],"answer":1,"explain":"Hash-based containers need both a hash function (to find buckets) and an equality comparison (to resolve collisions). Providing only one is insufficient."},
    {"type":"mcq","tag":"Specialize vs Overload","question":"Which is generally the LEAST recommended way to customize behavior for a specific type?","options":["Overloading a function template","Adding a non-template overload","Explicitly specializing a function template","Using tag dispatch"],"answer":2,"explain":"Explicitly specializing a function template is discouraged because of its non-obvious interaction with overload resolution (the Dimov/Abrahams issue). Overloads and tag dispatch are cleaner alternatives."},
    {"type":"code","tag":"Overload Resolution","question":"Which f is called by f(x) where x is const int?","code":"template <typename T> void f(T&);        // (1)\ntemplate <typename T> void f(const T&);  // (2)\n\nint main() { const int x = 0; f(x); }","options":["(1) with T = const int","(2) with T = int, being more specialized for const arguments","Ambiguous","Neither is viable"],"answer":1,"explain":"Both are viable, but f(const T&) is more specialized than f(T&) for a const argument, so partial ordering selects (2). (For a non-const argument, (1) would win as the better match.)"},
    {"type":"mcq","tag":"Specialization Basics","question":"std::vector<bool> famously differs from std::vector<T> because it is:","options":["A typedef for std::vector<char>","A full (explicit) specialization that packs bits, with a different interface and proxy references","A partial specialization of std::vector","The same template with no changes"],"answer":1,"explain":"std::vector<bool> is an explicit specialization that stores bits compactly and returns proxy objects rather than real bool& references — a well-known example (and cautionary tale) of how specializations can diverge from the primary."},
    {"type":"mcq","tag":"Partial Ordering","question":"Partial ordering is used to choose among function templates:","options":["Before checking conversion sequences","As a tie-breaker after conversion-sequence comparison, when two template candidates are otherwise equally good","Only when there are no non-template candidates","Never; it applies only to classes"],"answer":1,"explain":"Overload resolution first ranks by conversion sequences. Partial ordering (more-specialized-template) is one of the later tie-breakers used when the surviving best candidates are both templates."},
    {"type":"code","tag":"Ambiguity","question":"What happens for S<int*, double*> given these partial specializations?","code":"template <typename T, typename U> struct S;    // primary\ntemplate <typename T, typename U> struct S<T*, U>;  // (A)\ntemplate <typename T, typename U> struct S<T, U*>;  // (B)","options":["(A) is chosen","The instantiation is ambiguous and ill-formed, because neither (A) nor (B) is more specialized than the other","The primary template is used","(B) is chosen because it was declared last"],"answer":1,"explain":"int* matches (A)'s first-arg pattern and double* matches (B)'s second-arg pattern. Neither specialization is more specialized than the other for this argument set, so the choice is ambiguous — a hard error."},
    {"type":"mcq","tag":"SFINAE","question":"std::enable_if is commonly used to:","options":["Force a runtime branch","Conditionally remove a function template from the overload set via SFINAE when a compile-time condition is false","Speed up compilation","Replace virtual functions"],"answer":1,"explain":"enable_if yields a valid type only when its condition is true; when false, using it in a signature causes a substitution failure that (via SFINAE) drops that overload, shaping which candidates remain."},
    {"type":"mcq","tag":"Specialize vs Overload","question":"A practical advantage of overloading over specialization for functions is that overloads:","options":["Are resolved at runtime","Can differ in the number of parameters and participate fully in argument deduction and conversions, giving finer control","Never need declarations","Are always inlined"],"answer":1,"explain":"Overloads are independent functions that can have different arities and parameter types, and each takes part in deduction and conversion ranking. A specialization, by contrast, must match the primary's signature exactly."},
    {"type":"code","tag":"Template vs Non-template","question":"Which function is called by f(1L) (a long argument)?","code":"template <typename T> void f(T) {}   // (1)\nvoid f(int) {}                        // (2)\n\nint main() { f(1L); }","options":["(2) — after a standard long-to-int conversion","(1) — the template deduces T = long, an exact match, which beats the conversion needed by (2)","Ambiguous","(2) — non-templates are always preferred"],"answer":1,"explain":"The non-template f(int) requires a long-to-int conversion, while the template gives an exact match with T = long. A better conversion sequence wins outright, so the non-template preference tie-breaker never applies."},
    {"type":"mcq","tag":"Function Templates","question":"If you want 'template-like partial specialization' behavior for a function, the idiomatic replacement is:","options":["A macro","Additional overloaded function templates with more specialized parameter patterns","A full specialization","A virtual function"],"answer":1,"explain":"Since function templates cannot be partially specialized, you write extra function template overloads (e.g. one taking T* alongside one taking T). Partial ordering then selects the most specialized overload."},
    {"type":"mcq","tag":"Explicit Specialization","question":"An explicit specialization is 'used' (triggering the before-first-use rule) when:","options":["It is merely mentioned in a comment","The template is implicitly instantiated for those arguments, or the specialized entity is otherwise required to exist","The header is included","Never; there is no such rule"],"answer":1,"explain":"The rule keys off implicit instantiation (or any use requiring the entity). Once the compiler instantiates the primary for given arguments, a later specialization for those same arguments is too late."},
    {"type":"code","tag":"Partial Ordering","question":"Which overload is selected for f(v) where v is a std::vector<int>?","code":"#include <vector>\ntemplate <typename T> void f(T);                    // (1)\ntemplate <typename T> void f(std::vector<T>);       // (2)\n\nint main() { std::vector<int> v; f(v); }","options":["(1) with T = std::vector<int>","(2) with T = int, being more specialized","Ambiguous","(1), because (2) requires a copy"],"answer":1,"explain":"Both match, but f(std::vector<T>) is more specialized than the unconstrained f(T). Partial ordering picks (2), deducing T = int."},
    {"type":"mcq","tag":"Specialization Basics","question":"Which is TRUE about explicit specialization declarations?","options":["They introduce new template parameters that are deduced","They are not templates themselves in the usual sense — for a full specialization all parameters are fixed, so no deduction occurs on the specialization","They must be defined inline","They can only appear in headers"],"answer":1,"explain":"A full specialization pins every parameter, so it behaves like a concrete entity with nothing to deduce. (A partial specialization does keep some parameters, which are deduced during matching.)"},
    {"type":"mcq","tag":"SFINAE","question":"When SFINAE removes all template candidates and only a worse-matching non-template remains, the result is:","options":["A hard error, always","The non-template is selected if it is viable; SFINAE simply shrinks the candidate set","Ambiguity","The removed templates are restored"],"answer":1,"explain":"SFINAE only decides which candidates enter overload resolution. Whatever viable candidates remain — including non-templates — then compete normally; a lone viable non-template is chosen."},
    {"type":"code","tag":"Full Specialization","question":"Is this a valid explicit specialization of a function template?","code":"template <typename T> T max(T a, T b) { return b < a ? a : b; }\n\ntemplate <> const char* max<const char*>(const char* a, const char* b) {\n    return std::strcmp(a, b) < 0 ? b : a;\n}","options":["No, function templates cannot be specialized at all","Yes, it is a valid full specialization for T = const char*","No, the template<> syntax is wrong","Yes, but it is a partial specialization"],"answer":1,"explain":"Full (explicit) specialization of a function template is legal (only partial specialization is not). This defines max for const char* using strcmp. Whether it is a good idea is a separate question — an overload is usually preferred."},
    {"type":"mcq","tag":"Specialize vs Overload","question":"Which combination of features is the recommended toolkit for compile-time customization of functions?","options":["Function-template full specialization plus macros","Overloading, partial ordering, tag dispatch, and SFINAE/constraints — while avoiding function-template specialization","Only partial specialization of functions","Runtime virtual dispatch exclusively"],"answer":1,"explain":"Modern guidance favors overloads (ranked by partial ordering), tag dispatch, and SFINAE/concepts to steer selection cleanly, reserving specialization for class templates and avoiding the pitfalls of function-template specialization."},
    {"type":"mcq","tag":"std::hash","question":"A subtle requirement when writing operator() for a std::hash specialization is that it should:","options":["Return a different value each call","Be const-qualified, accept the key (typically by const ref), return std::size_t, and ideally be noexcept","Modify the key to cache the hash","Throw on unhashable input"],"answer":1,"explain":"The hash functor's call operator must be const and return std::size_t. Making it noexcept and taking the key by const reference matches standard-library expectations and enables optimizations."},
    {"type":"code","tag":"Partial Ordering","question":"Which template is more specialized here?","code":"template <typename T> void f(T, T);          // (A)\ntemplate <typename T> void f(T, int);        // (B)","options":["(A) is more specialized","(B) is more specialized (its second parameter is fixed as int)","They are equally specialized, so a call resolves to (A) by declaration order","Neither is more specialized, so a call matching both (e.g. f(1, 2)) is ambiguous"],"answer":3,"explain":"Partial ordering finds neither more specialized: (B) accepts (X, int) and (A) accepts (X, X), and neither argument set is a subset of the other. A call like f(1, 2) matches both equally, so it is ambiguous and ill-formed — confirmed by GCC and Clang. There is no declaration-order tie-breaker between templates."},
    {"type":"mcq","tag":"Partial Ordering","question":"If two function templates are found to be equally specialized after partial ordering and there is no other tie-breaker, the call is:","options":["Resolved by declaration order","Ambiguous and therefore ill-formed","Resolved by choosing the one with more parameters","Resolved in favor of the primary"],"answer":1,"explain":"Partial ordering is the last relevant tie-breaker for competing templates. If neither is more specialized and nothing else distinguishes them, overload resolution reports ambiguity."},
    {"type":"mcq","tag":"Specialization Basics","question":"Why can a partial specialization NOT be more general than the primary template?","options":["The compiler forbids more than one specialization","A partial specialization only refines the primary for a subset of arguments; if it matched everything (or more), it would replace or conflict with the primary, which is not its role","Because primaries are always final","Because it would be slower"],"answer":1,"explain":"A partial specialization must be strictly more specialized than the primary — it covers a narrower set of arguments. One that is equally or more general is rejected, since the primary already serves the general case."},
    {"type":"code","tag":"Partial Specialization","question":"How many template parameters does this partial specialization declare, and why?","code":"template <typename T, typename U> struct Pair {};\ntemplate <typename T> struct Pair<T, T> {};","options":["Two, matching the primary template","One: the specialization introduces the parameters it still needs; here both arguments are the same type T","Zero, because it is a full specialization","It is ill-formed"],"answer":1,"explain":"A partial specialization declares its own parameter list containing only the parameters it needs (one here), and lists the specialized argument pattern (T, T). Pair<int,int> matches it; Pair<int,double> uses the primary template."},
    {"type":"mcq","tag":"Function Templates","question":"The reason overloading integrates better than specialization for functions is that overload resolution:","options":["Ignores templates entirely","Considers all overloads (template and non-template) together and applies partial ordering, whereas a specialization is tied to exactly one base template","Runs at link time","Chooses randomly among candidates"],"answer":1,"explain":"Every overload is a first-class candidate ranked in one unified process. A specialization is bound to a specific base template and only matters if that base is the one selected — a fragile coupling overloading avoids."},
    {"type":"mcq","tag":"Explicit Specialization","question":"Placing an explicit specialization in a header that is included in multiple translation units requires that:","options":["It be marked static","The same specialization definition is seen consistently, and function-template specializations that are not inline should be defined in one TU to avoid ODR violations","It be defined only in main()","It never be included more than once, even with guards"],"answer":1,"explain":"Like ordinary functions, a non-inline explicit function-template specialization defined in a header included in several TUs risks multiple-definition (ODR) errors; declare it in the header and define it once, or mark it inline."},
    {"type":"code","tag":"Partial Ordering","question":"Given both partial specializations, which is used for S<int*, int*>?","code":"template <typename T, typename U> struct S;   // primary\ntemplate <typename T> struct S<T, T>;         // (A)\ntemplate <typename T> struct S<T*, T*>;       // (B)","options":["(A), because it was declared first","(B), because S<T*,T*> is more specialized than S<T,T>","The primary template","Ambiguous, so ill-formed"],"answer":1,"explain":"S<int*, int*> matches both (A) with T=int* and (B) with T=int. Partial ordering of class template specializations picks the more specialized (B), just as with function partial ordering."},
    {"type":"mcq","tag":"Tag Dispatch","question":"Tag dispatch and full function-template specialization both aim to vary behavior by type, but tag dispatch is preferred because it:","options":["Uses runtime type information","Relies on ordinary overload resolution and partial ordering, avoiding the base-template-binding pitfall of specialization","Requires fewer functions in all cases","Cannot fail to compile"],"answer":1,"explain":"Because tag dispatch is just overloading on tag types, it plays by overload resolution's normal rules and sidesteps the surprising interaction between specializations and base-template selection."},
    {"type":"mcq","tag":"Specialize vs Overload","question":"When designing a customization point for user types, a common modern approach that avoids function-template specialization is:","options":["Requiring users to edit your template","Providing an overloaded free function found via ADL (or a customization-point object / trait class users specialize)","Using global variables","Forcing runtime polymorphism"],"answer":1,"explain":"Libraries expose ADL-found free-function overloads or specializable trait classes (like std::hash) rather than asking users to specialize a function template, keeping customization robust and resolution predictable."},
    {"type":"code","tag":"Template vs Non-template","question":"Which is called by print(\"hi\")?","code":"template <typename T> void print(T);   // (1)\nvoid print(const char*);               // (2)\n\nint main() { print(\"hi\"); }","options":["(1) with T = const char* — templates are exact","(2) — both match, and the non-template wins the tie","Ambiguous","(1), because string literals prefer templates"],"answer":1,"explain":"The literal is const char[3], which decays to const char* for both candidates — equally good conversions. The tie is broken in favor of the non-template print(const char*)."},
    {"type":"mcq","tag":"Partial Ordering","question":"In partial ordering, the compiler transforms each template by:","options":["Deleting its parameters","Substituting unique synthesized types for its parameters, then attempting deduction against the other template's parameters","Sorting them alphabetically","Counting parameters only"],"answer":1,"explain":"Each template's parameters are replaced by unique invented types to form concrete argument lists; the compiler then checks whether the other template can deduce from them. Success in one direction only establishes which is more specialized."},
    {"type":"mcq","tag":"std::hash","question":"A pitfall of relying on std::hash specialization for equality-based lookup is forgetting that:","options":["Hashes must be unique per object","Equal keys MUST produce equal hashes; if operator== and the hash disagree, unordered containers behave incorrectly","std::hash caches results automatically","Hashes must be sorted"],"answer":1,"explain":"The hash and equality must be consistent: whenever a == b, hash(a) must equal hash(b). Violating this invariant breaks lookups in unordered_map/unordered_set even though the code compiles."},
    {"type":"code","tag":"Overload Resolution","question":"Which overload wins for h(0)?","code":"template <typename T> void h(T);    // (1)\ntemplate <typename T> void h(T*);   // (2)\n\nint main() { h(0); }","options":["(2), because 0 is a null pointer constant","(1) with T = int; 0 is deduced as int, and deduction never treats a literal 0 as a pointer","Ambiguous","Neither is viable"],"answer":1,"explain":"During template argument deduction, the literal 0 has type int; it is not deduced as a pointer even though it could convert to one. Only (1) deduces successfully, giving T = int."},
    {"type":"mcq","tag":"Specialization Basics","question":"Which best summarizes the guidance from this topic?","options":["Always specialize function templates for performance","Prefer overloading for function customization; use full and partial specialization for class templates; never partially specialize functions (it is illegal)","Never use specialization of any kind","Partial specialization works for both functions and classes"],"answer":1,"explain":"The core lessons: function templates support only full specialization (use overloading instead for customization), while class templates support both full and partial specialization — the workhorse of template metaprogramming."},
    {"type":"mcq","tag":"Overload Resolution","question":"When both a function template and a non-template function require an identical user-defined conversion to match the argument, overload resolution:","options":["Fails, because two conversions tie","Still prefers the non-template function once the conversion sequences are found equally good","Prefers the template because it is generic","Picks by declaration order"],"answer":1,"explain":"The non-template-vs-template tie-breaker applies whenever the conversion sequences are indistinguishable, regardless of whether that sequence is exact or a user-defined conversion. The non-template wins."},
    {"type":"code","tag":"Full Specialization","question":"Does this compile?","code":"template <typename T> struct A { void f() {} };\ntemplate <> struct A<int>;          // declaration of specialization\nA<int>* p;                          // pointer only\n// no A<int> object or member use","options":["No: A<int> must be fully defined before use","Yes: declaring a specialization (without defining it) is fine as long as you only use it in ways that need no complete type, like forming a pointer","No: template<> struct A<int>; is invalid syntax","Yes, but p is a null pointer by default"],"answer":1,"explain":"You can declare a class-template specialization without defining it. As long as A<int> is used only where an incomplete type suffices (e.g. a pointer), no definition is required."},
    {"type":"mcq","tag":"Partial Ordering","question":"Partial ordering of function templates is performed only:","options":["Before argument deduction","Among the candidates that remain after deduction and conversion ranking have already tied","Only for variadic templates","Whenever a non-template exists"],"answer":1,"explain":"Deduction and conversion-sequence comparison happen first; partial ordering is invoked as a tie-breaker among surviving equally-good template candidates."},
    {"type":"mcq","tag":"Partial Specialization","question":"Which is a legitimate use of class-template partial specialization?","options":["Changing a function's return type based on arity","Implementing type traits like is_pointer or remove_reference by matching structural patterns of the argument","Overloading operator+","Selecting a runtime branch"],"answer":1,"explain":"Partial specialization matches structural patterns (T*, T&, const T, C<Args...>) and is the standard mechanism for building type traits at compile time."},
    {"type":"code","tag":"Member Specialization","question":"What is the effect of this code?","code":"template <typename T> struct Stack {\n    void push(T v);\n    void print() const;\n};\n\ntemplate <> void Stack<bool>::print() const { /* packed output */ }","options":["It specializes the entire Stack<bool>","It provides a custom print() only for Stack<bool>; push() and everything else still come from the primary template","It is ill-formed because you cannot specialize one member","It specializes print() for every Stack"],"answer":1,"explain":"Only Stack<bool>::print gets a special definition. Stack<bool>::push and other members are still generated from the primary template, so member specialization lets you override behavior surgically."},
    {"type":"mcq","tag":"Full Specialization","question":"A full specialization of a class template and the primary template share:","options":["Nothing at all","The template name and the number/kind of template parameters, but not necessarily any members","Every member function","The same object size"],"answer":1,"explain":"Only the name and parameter shape must match; the specialization is otherwise an independent definition that may declare entirely different members."},
    {"type":"mcq","tag":"Specialize vs Overload","question":"Why can adding an overload change behavior that a function-template specialization cannot?","options":["Overloads run at runtime","An overload is an independent candidate in overload resolution and can be more specialized via partial ordering, whereas a specialization only refines an already-chosen base template","Overloads ignore const","Specializations are slower"],"answer":1,"explain":"Overloads participate directly in resolution and ordering; a specialization is bound to one base template and only matters if that base is selected."},
    {"type":"mcq","tag":"Explicit Specialization","question":"The 'must appear before first use' rule for explicit specializations exists to guarantee:","options":["Faster compilation","That the compiler never implicitly instantiates the primary for arguments that are later specialized differently, which would violate the ODR","Smaller binaries","Thread safety"],"answer":1,"explain":"If the primary were implicitly instantiated first and then specialized, two different definitions for the same arguments could exist — an ODR violation the rule prevents."},
    {"type":"mcq","tag":"SFINAE","question":"Which context is an 'immediate context' where a substitution failure triggers SFINAE rather than a hard error?","options":["Inside the function body","In the function's template parameter list, parameter types, or return type","In a member function called by the body","In a base class constructor"],"answer":1,"explain":"SFINAE applies only to failures in the signature (parameters, return type, template parameters). Failures arising from instantiating the body are hard errors."},
    {"type":"mcq","tag":"std::hash","question":"When you provide a hash for a user type via a separate functor passed to unordered_map (instead of specializing std::hash), you:","options":["Must still specialize std::hash as well","Supply the functor as the container's Hash template argument, avoiding any change to namespace std","Cannot use operator==","Lose equality checking"],"answer":1,"explain":"Passing a custom hash functor as the container's Hash parameter is an alternative to specializing std::hash and keeps you out of namespace std entirely."},
    {"type":"mcq","tag":"Tag Dispatch","question":"A tag type used for tag dispatch is typically:","options":["A large object with data members","An empty struct whose only purpose is to select an overload at compile time","A runtime enum value","A pointer to a virtual table"],"answer":1,"explain":"Tags are usually empty structs (like std::true_type or the iterator category tags); they carry no data and exist purely to steer overload resolution."}
  ]
};
